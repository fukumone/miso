# miso

WebSocket Demo Application

# To Install and Run:

```
$ cp config/database.yml.sample config/database.yml
$ rake db:create
$ rake db:migrate

$ bundle install

$ rackup
# open localhost:9292
```
